# https://github.com/actions/jekyll-build-pages
name: Deploy Jekyll with GitHub Pages for EdgeOne

on:
  push:
    branches:
      - main

  issues:
    types: [opened, edited, deleted, pinned, unpinned, labeled, unlabeled, closed, reopened, assigned, unassigned]  # issues事件触发
    
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      EDGEONE_API_TOKEN: ${{ secrets.EDGEONE_API_TOKEN }}
      PERSONAL_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
      # GOOGLE_SCHOLAR_ID: ${{ secrets.GOOGLE_SCHOLAR_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site

      - name: Upload artifact (For debug)
        uses: actions/upload-pages-artifact@v3

      - name: Deploy to EdgeOne Pages
        run: npx edgeone pages deploy ./_site -n jpc-cv -t ${{ secrets.EDGEONE_API_TOKEN }} -e production 
        env:
          EDGEONE_API_TOKEN: ${{ secrets.EDGEONE_API_TOKEN }}
